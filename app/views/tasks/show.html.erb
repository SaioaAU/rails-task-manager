<h1><%= @task.title %></h1>
<h3><%= @task.details %></h3>
<p><%= @task.completed %></p>

<%= link_to "back to index", tasks_path %>

<%= link_to "edit this task", edit_path(@task) %>

<%= link_to "delete this form", delete_path(@task), method: :delete, data: {confirm: "Are you sure?"} %>
